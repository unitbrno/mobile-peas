<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/coordinator_layout"
    android:background="@color/white"
    tools:context=".activities.DetailRecordActivity">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/detail_record"
            style="@style/TitleStyle"
            android:text="@string/title_detail_record"
            tools:ignore="MissingPrefix" />

        <android.support.v7.widget.RecyclerView
            android:id="@+id/recycler_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_above="@id/btn_back"
            android:layout_below="@id/detail_record" />

        <android.support.v7.widget.AppCompatButton
            android:id="@+id/btn_back"
            style="@style/BordelessButtonStyle"
            android:layout_alignParentBottom="true"
            android:drawableEnd="@android:color/transparent"
            android:drawableStart="@drawable/ic_keyboard_arrow_left_black_24dp"
            android:text="@string/btn_back" />


    </RelativeLayout>


</android.support.design.widget.CoordinatorLayout>